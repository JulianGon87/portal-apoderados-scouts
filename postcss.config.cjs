// Archivo: postcss.config.cjs (Versión Final)

module.exports = {
  plugins: {
    // Usamos 'tailwindcss' y 'autoprefixer', que deberían ser resueltos 
    // por la nueva dependencia instalada y Vercel.
    tailwindcss: {},
    autoprefixer: {},
  },
}